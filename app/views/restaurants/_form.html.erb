<%= form_for @restaurant do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name, placeholder: 'Enter restaurant name' %>
	<%= f.text_field :name, placeholder: 'Enter cuisine type' %>
	<%= f.submit  'Add' %>
<% end %>